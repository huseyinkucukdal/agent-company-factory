"use client";

import LinksPage from "../../../links/page";

export default function CompanyLinksPage() {
  // For now, the global links page renders all links — filtering by company
  // can be added once the API supports a query parameter.
  return <LinksPage />;
}
